# sampleDjango-Prac
- Initialized Django project structure - Implemented sample endpoints (greet, info, dynamic query, calculator) - Added math operations (add, sub, mul, div) handled via URL - Tested locally and verified basic functionality - Prepped for deployment to GitHub repository
